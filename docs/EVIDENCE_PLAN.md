# Evidence plan
Evidence is pending; no screenshots or runtime claims are fabricated.

1. Build on documented Linux and serve `fixtures` with the local HTTP server.
2. Capture rendering screenshots at a fixed viewport and after a narrow resize, recording OS, backend, viewport, and commit.
3. Capture task-list states after load, add, toggle, remove, and each filter; record console/network output proving `tasks.json` fetch.
4. Open one permitted local/real page separately and record URL, timestamp, and limitations; do not infer remote compatibility.
5. Run each recovery fixture with a bounded timeout and record containment/reporting.
6. Attach raw commands and artifacts only after a human repeats them.
