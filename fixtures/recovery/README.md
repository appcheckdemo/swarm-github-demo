# Recovery fixtures
`malformed.html` has unclosed markup, `script-exception.html` throws, `infinite-loop.html` needs a timeout/sandbox, and `missing-resource.html` references absent files. Hosts should report or contain failures rather than crashing the session.
