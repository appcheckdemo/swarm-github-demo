# Rendering fixture
Open `index.html` from a local server. It has no remote dependencies and covers headings, text, links, nested lists, local image, table, form, boxes, wrapping, cascade, backgrounds, borders, overflow, flex/grid, positioning, and an anchor. Resize the viewport to exercise wrapping. The button intentionally has no behavior.
