# Task-list fixture
Serve this directory over HTTP so relative asynchronous `fetch('tasks.json')` works. Two seeded tasks load; Add creates, checkboxes toggle, Remove deletes, and All/Open/Done filter through DOM events. Edits are not persisted.
